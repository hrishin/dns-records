# BIND DNS Provider Configuration

dns_providers:
  bind:
    nameserver: 127.0.0.1
    port: 53
    key_file: bind/update-key.conf
    key_name: update-key
    # zone_file: /etc/bind/zones/ib.bigbank.com.zone  # Uncomment if using zone files, as a fallback if zone transfer fails

default_provider: bind

logging:
  level: INFO
  file: dns_manager_bind.log
